{
  "extends": [
    "next/core-web-vitals",
    "next/typescript"
  ],
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "drizzle-orm/postgres-js",
            "message": "Layer 2 violation. All database access goes through lib/db/scoped.ts. Only lib/db/client.ts may construct a Drizzle client."
          },
          {
            "name": "postgres",
            "message": "Layer 2 violation. Only lib/db/client.ts may open a Postgres connection."
          }
        ],
        "patterns": [
          {
            "group": [
              "**/lib/db/client",
              "@/lib/db/client",
              "../client",
              "./client"
            ],
            "message": "Layer 2 violation. The raw Drizzle client is unscoped and bypasses RLS session variables. Import withScope from lib/db/scoped.ts instead."
          },
          {
            "group": [
              "**/lib/security/aes",
              "@/lib/security/aes"
            ],
            "message": "Only lib/security/field-encryption.ts may import the AES primitives. Use encryptField/decryptField."
          }
        ]
      }
    ],
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[callee.property.name='createDecipheriv']",
        "message": "AES primitives are confined to lib/security/aes.ts. Use decryptField from lib/security/field-encryption.ts."
      },
      {
        "selector": "CallExpression[callee.name='createDecipheriv']",
        "message": "AES primitives are confined to lib/security/aes.ts. Use decryptField from lib/security/field-encryption.ts."
      }
    ]
  },
  "overrides": [
    {
      "files": [
        "lib/db/scoped.ts",
        "lib/db/client.ts",
        "lib/jobs/**/*.ts",
        "scripts/**/*.ts",
        "tests/**/*.ts",
        "drizzle.config.ts"
      ],
      "rules": {
        "no-restricted-imports": "off"
      }
    },
    {
      "files": [
        "lib/security/aes.ts",
        "lib/security/field-encryption.ts"
      ],
      "rules": {
        "no-restricted-syntax": "off"
      }
    }
  ]
}
